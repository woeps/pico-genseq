#pragma once

namespace sequencer {

    // Enum for pattern playback modes
    enum class PlayMode {
        FORWARD,
        BACKWARD,
        PENDULUM,
        RANDOM
    };

} // namespace sequencer
